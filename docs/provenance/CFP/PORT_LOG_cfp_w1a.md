- ErdosProblems/Erdos186/CFP/Bilu/Proposition75Case2Construction.lean: `Mathlib.Analysis.InnerProductSpace.NormDet`'s `List.TFAE.out` switched from 0-indexed to **1-indexed** statement positions between v4.33 and v4.34.0-rc2 (see doc comment on `TFAE.out` in `Mathlib/Data/List/TFAE.lean`: "i, j are the 1-indexed indices for TFAE statements... must be greater than 0"). `f.normDet_ne_zero_tfae.out 0 4` (old 0-indexed: 1st stmt `f.normDet ≠ 0` and 5th stmt `Function.Injective f`) failed to synthesize the default `h₂ : l[j-1]? = some b` autoparam because index 4 no longer pointed at the 5th list element. Fixed by converting to 1-indexed: `f.normDet_ne_zero_tfae.out 1 5`. No other `.out i j` calls in this file.
